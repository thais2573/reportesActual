{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('build/css/select2.min.css') }}">
{% endblock %}
{% block main %}


    <div class="card">
        <div class="card-header">
            <strong>Adicionar equipo</strong><br>

        </div>
        <div class="card-body">
            {% if tipo_edicion=='inventario_nuevo' %}

        {% else %}
            <form class="form-inline float-right" method="post">
                <a  href="{{ path('buscar_equipo',{'inventario':inventario[0].id }) }}">
                    {% endif %}
                    <div class="row">
                        <div class="input-group float-right col-6" id="buscador">
                            <input class="form-control col-5 " id="numI" type="text" name="numInv" placeholder="Numero de inventario">
                            <span class="input-group-prepend">
                              <button class="btn btn-primary busca" id="buscarEquipo1" type="button">
                                <i class="fa fa-search"></i> </button>
                            </span>
                        </div>

                        <div class="float-right col-3">
                            <strong>Lista Temporal de equipos</strong> <button class="btn btn-success"  id="guardaEquipos" style="display: none">Guardar Equipos</button>
                        </div>
                        <div class="col-3" id="mensaje">
                          <span class="badge badge-danger">
                            Este equipo ya esta asignado a una estacion
                          </span>
                        </div>
                    </div>
                    <div class="container col-6 float-right" id="nuevosEq" style="border: dot-dash;border-bottom: #0a0a0a">

                    </div>
                </a>
            </form>

            <br> <br> <br>
            {#        {{ dump(nuevo) }}#}
            {#          {% set equiposNuevos=[] %}#}
            {#      {% if nuevo!=null %}#}
            {#        <div class="equipos">#}

            {#        </div>#}
            {#        <label>Equipo encontrado:</label> <label>{{ nuevo.tipoEquipo }}</label>#}
            {#        <label>#Inv:{{ nuevo.numInventario }}</label>#}
            {#        <label>Modelo:{{ nuevo.modelo }}</label>#}
            {#        <label>{% if nuevo.estacion ==null %}#}
            {#            <button type="button" class="btn btn-ghost-success" id="agregarEquipo">Agregar a estacion</button>#}
            {#          {% else %}#}
            {#            <span class="badge badge-danger">Este equipo ya esta asignado a una estacion</span>#}
            {#          {% endif %}</label>#}

            {#      {% endif %}#}
            </form>
        </div>
    </div>

{% endblock %}

